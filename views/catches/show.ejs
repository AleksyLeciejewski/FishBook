<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= catchItem.species %> - FishBook</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .comment-card {
            transition: all 0.2s ease;
        }
        .comment-card:hover {
            background-color: #f9fafb;
        }
        .like-button.liked {
            color: #ef4444;
        }
        .like-button.liked i {
            animation: heartBeat 0.3s ease;
        }
        @keyframes heartBeat {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.2); }
        }
    </style>
</head>
<body class="bg-gradient-to-br from-blue-50 to-indigo-100 min-h-screen">
    <!-- Navigation -->
    <nav class="bg-gradient-to-r from-blue-600 to-indigo-600 text-white shadow-lg">
        <div class="container mx-auto px-4 py-3 flex justify-between items-center">
            <a href="/" class="text-2xl font-bold flex items-center">
                <i class="fas fa-fish mr-2"></i>FishBook
            </a>
            <div class="flex items-center space-x-4">
                <% if (locals.currentUser) { %>
                    <a href="/catches" class="hover:bg-white hover:bg-opacity-20 px-3 py-2 rounded transition">My Catches</a>
                    <a href="/trips" class="hover:bg-white hover:bg-opacity-20 px-3 py-2 rounded transition">Trips</a>
                    <a href="/fishing-spots" class="hover:bg-white hover:bg-opacity-20 px-3 py-2 rounded transition">Find Spots</a>
                    <div class="relative group">
                        <button class="flex items-center space-x-2 focus:outline-none hover:bg-white hover:bg-opacity-20 px-3 py-2 rounded transition">
                            <img src="<%= currentUser.profilePicture || '/images/default-avatar.png' %>" 
                                 alt="Profile" 
                                 class="w-8 h-8 rounded-full border-2 border-white">
                            <a href="/users/<%= currentUser._id %>" class="hover:underline"><%= currentUser.username %></a>
                            <i class="fas fa-chevron-down text-xs"></i>
                        </button>
                        <div class="hidden group-hover:block absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-xl py-2 z-10">
                            <a href="/users/<%= currentUser._id %>" class="block px-4 py-2 text-gray-800 hover:bg-blue-50 transition">
                                <i class="fas fa-user mr-2 text-blue-600"></i>Profile
                            </a>
                            <a href="/settings" class="block px-4 py-2 text-gray-800 hover:bg-blue-50 transition">
                                <i class="fas fa-cog mr-2 text-blue-600"></i>Settings
                            </a>
                            <form action="/auth/logout" method="POST" class="border-t border-gray-200 mt-1 pt-1">
                                <button type="submit" class="w-full text-left px-4 py-2 text-gray-800 hover:bg-red-50 transition">
                                    <i class="fas fa-sign-out-alt mr-2 text-red-600"></i>Logout
                                </button>
                            </form>
                        </div>
                    </div>
                <% } else { %>
                    <a href="/auth/register" class="hover:bg-white hover:bg-opacity-20 px-3 py-2 rounded transition">Register</a>
                    <a href="/auth/login" class="bg-white text-blue-600 hover:bg-gray-100 px-4 py-2 rounded-lg font-medium transition shadow-md">Login</a>
                <% } %>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="container mx-auto px-4 py-8">
        <!-- Back Button -->
        <div class="mb-6">
            <a href="/catches" class="inline-flex items-center text-blue-600 hover:text-blue-800 font-medium transition">
                <i class="fas fa-arrow-left mr-2"></i> Back to All Catches
            </a>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <!-- Main Content -->
            <div class="lg:col-span-2">
                <!-- Catch Card -->
                <div class="bg-white rounded-xl shadow-lg overflow-hidden">
                    <!-- Image -->
                    <div class="relative h-96 bg-gradient-to-br from-blue-400 to-indigo-500">
                        <% if (catchItem.image) { %>
                            <img src="/<%= catchItem.image %>" 
                                 alt="<%= catchItem.species %>" 
                                 class="w-full h-full object-cover">
                        <% } else { %>
                            <div class="flex items-center justify-center h-full">
                                <i class="fas fa-fish text-white text-8xl opacity-50"></i>
                            </div>
                        <% } %>
                        
                        <!-- Species Badge -->
                        <div class="absolute top-6 left-6">
                            <span class="bg-white bg-opacity-95 text-gray-800 px-4 py-2 rounded-full text-lg font-bold shadow-lg">
                                <i class="fas fa-fish mr-2 text-blue-600"></i> <%= catchItem.species %>
                            </span>
                        </div>
                    </div>

                    <!-- Content -->
                    <div class="p-8">
                        <!-- User Info -->
                        <div class="flex items-center justify-between mb-6">
                            <div class="flex items-center space-x-3">
                                <img src="<%= catchItem.user.profilePicture || '/images/default-avatar.png' %>" 
                                     alt="<%= catchItem.user.username %>" 
                                     class="w-12 h-12 rounded-full border-2 border-gray-200">
                                <div>
                                    <a href="/users/<%= catchItem.user._id %>" class="font-bold text-gray-900 hover:text-blue-600 block">
                                        <%= catchItem.user.username %>
                                    </a>
                                    <time class="text-sm text-gray-500" datetime="<%= new Date(catchItem.dateCaught).toISOString() %>">
                                        <%= new Date(catchItem.dateCaught).toLocaleDateString('en-US', { 
                                            year: 'numeric',
                                            month: 'long', 
                                            day: 'numeric' 
                                        }) %>
                                    </time>
                                </div>
                            </div>
                            
                            <!-- Like Button -->
                            <% if (locals.currentUser) { %>
                                <button id="likeButton" 
                                        class="like-button flex items-center space-x-2 px-6 py-3 rounded-lg font-semibold transition transform hover:scale-105 <%= catchItem.likes.some(like => String(like) === String(currentUser._id)) ? 'liked bg-red-50 text-red-500' : 'bg-gray-100 text-gray-600 hover:bg-red-50 hover:text-red-500' %>"
                                        data-catch-id="<%= catchItem._id %>">
                                    <i class="<%= catchItem.likes.some(like => String(like) === String(currentUser._id)) ? 'fas' : 'far' %> fa-heart text-xl"></i>
                                    <span id="likeCount"><%= catchItem.likes.length %></span>
                                </button>
                            <% } else { %>
                                <div class="flex items-center space-x-2 px-6 py-3 bg-gray-100 rounded-lg text-gray-600">
                                    <i class="far fa-heart text-xl"></i>
                                    <span><%= catchItem.likes.length %></span>
                                </div>
                            <% } %>
                        </div>

                        <!-- Stats Grid -->
                        <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-6">
                            <div class="bg-blue-50 rounded-lg p-4 text-center">
                                <i class="fas fa-weight-hanging text-2xl text-blue-600 mb-2"></i>
                                <div class="text-2xl font-bold text-gray-900"><%= catchItem.weight %> kg</div>
                                <div class="text-sm text-gray-600">Weight</div>
                            </div>
                            <% if (catchItem.length) { %>
                                <div class="bg-green-50 rounded-lg p-4 text-center">
                                    <i class="fas fa-ruler-vertical text-2xl text-green-600 mb-2"></i>
                                    <div class="text-2xl font-bold text-gray-900"><%= catchItem.length %> cm</div>
                                    <div class="text-sm text-gray-600">Length</div>
                                </div>
                            <% } %>
                            <div class="bg-purple-50 rounded-lg p-4 text-center">
                                <i class="far fa-comment text-2xl text-purple-600 mb-2"></i>
                                <div class="text-2xl font-bold text-gray-900"><%= catchItem.comments.length %></div>
                                <div class="text-sm text-gray-600">Comments</div>
                            </div>
                            <div class="bg-red-50 rounded-lg p-4 text-center">
                                <i class="far fa-heart text-2xl text-red-600 mb-2"></i>
                                <div class="text-2xl font-bold text-gray-900"><%= catchItem.likes.length %></div>
                                <div class="text-sm text-gray-600">Likes</div>
                            </div>
                        </div>

                        <!-- Description -->
                        <% if (catchItem.description) { %>
                            <div class="mb-6">
                                <h3 class="text-lg font-bold text-gray-900 mb-2">Description</h3>
                                <p class="text-gray-700 leading-relaxed"><%= catchItem.description %></p>
                            </div>
                        <% } %>

                        <!-- Location -->
                        <div class="mb-6">
                            <h3 class="text-lg font-bold text-gray-900 mb-2">
                                <i class="fas fa-map-marker-alt text-red-500 mr-2"></i>Location
                            </h3>
                            <p class="text-gray-700"><%= catchItem.location.name %></p>
                        </div>

                        <!-- Additional Details -->
                        <% if (catchItem.bait || catchItem.tackle) { %>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
                                <% if (catchItem.bait) { %>
                                    <div>
                                        <h4 class="text-sm font-bold text-gray-900 mb-1">
                                            <i class="fas fa-worm text-orange-500 mr-2"></i>Bait
                                        </h4>
                                        <p class="text-gray-700"><%= catchItem.bait %></p>
                                    </div>
                                <% } %>
                                <% if (catchItem.tackle) { %>
                                    <div>
                                        <h4 class="text-sm font-bold text-gray-900 mb-1">
                                            <i class="fas fa-fishing-rod text-blue-500 mr-2"></i>Tackle
                                        </h4>
                                        <p class="text-gray-700"><%= catchItem.tackle %></p>
                                    </div>
                                <% } %>
                            </div>
                        <% } %>

                        <!-- Weather Info -->
                        <% if (catchItem.weather && catchItem.weather.condition) { %>
                            <div class="bg-gradient-to-r from-blue-50 to-indigo-50 rounded-lg p-4">
                                <h3 class="text-lg font-bold text-gray-900 mb-3">
                                    <i class="fas fa-cloud-sun text-yellow-500 mr-2"></i>Weather Conditions
                                </h3>
                                <div class="grid grid-cols-2 md:grid-cols-4 gap-4 text-sm">
                                    <% if (catchItem.weather.temperature) { %>
                                        <div>
                                            <span class="text-gray-600">Temperature:</span>
                                            <span class="font-semibold text-gray-900 ml-1"><%= catchItem.weather.temperature %>°C</span>
                                        </div>
                                    <% } %>
                                    <% if (catchItem.weather.condition) { %>
                                        <div>
                                            <span class="text-gray-600">Condition:</span>
                                            <span class="font-semibold text-gray-900 ml-1"><%= catchItem.weather.condition %></span>
                                        </div>
                                    <% } %>
                                    <% if (catchItem.weather.windSpeed) { %>
                                        <div>
                                            <span class="text-gray-600">Wind:</span>
                                            <span class="font-semibold text-gray-900 ml-1"><%= catchItem.weather.windSpeed %> km/h</span>
                                        </div>
                                    <% } %>
                                    <% if (catchItem.weather.windDirection) { %>
                                        <div>
                                            <span class="text-gray-600">Direction:</span>
                                            <span class="font-semibold text-gray-900 ml-1"><%= catchItem.weather.windDirection %></span>
                                        </div>
                                    <% } %>
                                </div>
                            </div>
                        <% } %>
                    </div>
                </div>

                <!-- Comments Section -->
                <div class="bg-white rounded-xl shadow-lg p-8 mt-8">
                    <h2 class="text-2xl font-bold text-gray-900 mb-6">
                        <i class="far fa-comments text-blue-600 mr-2"></i>
                        Comments (<span id="commentCount"><%= catchItem.comments.length %></span>)
                    </h2>

                    <!-- Add Comment Form -->
                    <% if (locals.currentUser) { %>
                        <form id="commentForm" class="mb-8">
                            <div class="flex space-x-4">
                                <img src="<%= currentUser.profilePicture || '/images/default-avatar.png' %>" 
                                     alt="<%= currentUser.username %>" 
                                     class="w-10 h-10 rounded-full border-2 border-gray-200 flex-shrink-0">
                                <div class="flex-1">
                                    <textarea id="commentText" 
                                              name="text" 
                                              rows="3" 
                                              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition resize-none" 
                                              placeholder="Share your thoughts..."
                                              maxlength="500"
                                              required></textarea>
                                    <div class="flex justify-between items-center mt-2">
                                        <span id="charCount" class="text-sm text-gray-500">0 / 500</span>
                                        <button type="submit" 
                                                class="bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700 text-white px-6 py-2 rounded-lg font-medium transition transform hover:scale-105 shadow-md">
                                            <i class="fas fa-paper-plane mr-2"></i>Post Comment
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </form>
                    <% } else { %>
                        <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-8 text-center">
                            <p class="text-gray-700">
                                <a href="/auth/login" class="text-blue-600 hover:text-blue-800 font-semibold">Login</a> 
                                or 
                                <a href="/auth/register" class="text-blue-600 hover:text-blue-800 font-semibold">Register</a> 
                                to leave a comment
                            </p>
                        </div>
                    <% } %>

                    <!-- Comments List -->
                    <div id="commentsList" class="space-y-4">
                        <% if (catchItem.comments && catchItem.comments.length > 0) { %>
                            <% catchItem.comments.forEach(comment => { %>
                                <div class="comment-card flex space-x-4 p-4 rounded-lg border border-gray-100">
                                    <img src="<%= comment.user.profilePicture || '/images/default-avatar.png' %>" 
                                         alt="<%= comment.user.username %>" 
                                         class="w-10 h-10 rounded-full border-2 border-gray-200 flex-shrink-0">
                                    <div class="flex-1">
                                        <div class="flex items-center justify-between mb-1">
                                            <a href="/users/<%= comment.user._id %>" class="font-semibold text-gray-900 hover:text-blue-600">
                                                <%= comment.user.username %>
                                            </a>
                                            <time class="text-sm text-gray-500" datetime="<%= new Date(comment.createdAt).toISOString() %>">
                                                <%= new Date(comment.createdAt).toLocaleDateString('en-US', { 
                                                    month: 'short', 
                                                    day: 'numeric',
                                                    year: 'numeric'
                                                }) %>
                                            </time>
                                        </div>
                                        <p class="text-gray-700"><%= comment.text %></p>
                                    </div>
                                </div>
                            <% }); %>
                        <% } else { %>
                            <div class="text-center py-8 text-gray-500">
                                <i class="far fa-comment-dots text-4xl mb-3 opacity-50"></i>
                                <p>No comments yet. Be the first to comment!</p>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>

            <!-- Sidebar -->
            <div class="lg:col-span-1">
                <!-- Map Preview -->
                <div class="bg-white rounded-xl shadow-lg p-6 mb-6">
                    <h3 class="text-lg font-bold text-gray-900 mb-4">
                        <i class="fas fa-map text-blue-600 mr-2"></i>Location
                    </h3>
                    <div class="bg-gray-200 rounded-lg h-48 flex items-center justify-center">
                        <i class="fas fa-map-marker-alt text-4xl text-red-500"></i>
                    </div>
                    <p class="text-sm text-gray-600 mt-3">
                        <i class="fas fa-map-marker-alt text-red-500 mr-1"></i>
                        <%= catchItem.location.name %>
                    </p>
                    <p class="text-xs text-gray-500 mt-1">
                        Coordinates: <%= catchItem.location.coordinates[1].toFixed(4) %>, <%= catchItem.location.coordinates[0].toFixed(4) %>
                    </p>
                </div>

                <!-- Similar Catches -->
                <div class="bg-white rounded-xl shadow-lg p-6">
                    <h3 class="text-lg font-bold text-gray-900 mb-4">
                        <i class="fas fa-fish text-blue-600 mr-2"></i>More from <%= catchItem.user.username %>
                    </h3>
                    <p class="text-sm text-gray-500 text-center py-4">
                        Coming soon...
                    </p>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t border-gray-200 mt-12">
        <div class="container mx-auto px-4 py-8">
            <div class="text-center text-gray-600">
                <p class="text-sm">
                    &copy; <%= new Date().getFullYear() %> FishBook. Made with <i class="fas fa-heart text-red-500"></i> by anglers, for anglers.
                </p>
            </div>
        </div>
    </footer>

    <script>
        // Like functionality
        const likeButton = document.getElementById('likeButton');
        if (likeButton) {
            likeButton.addEventListener('click', async function() {
                const catchId = this.dataset.catchId;
                
                try {
                    const response = await fetch(`/catches/${catchId}/like`, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        }
                    });

                    if (response.ok) {
                        const data = await response.json();
                        const likeCount = document.getElementById('likeCount');
                        const icon = this.querySelector('i');
                        
                        likeCount.textContent = data.likes.length;
                        
                        if (this.classList.contains('liked')) {
                            this.classList.remove('liked', 'bg-red-50', 'text-red-500');
                            this.classList.add('bg-gray-100', 'text-gray-600');
                            icon.classList.remove('fas');
                            icon.classList.add('far');
                        } else {
                            this.classList.add('liked', 'bg-red-50', 'text-red-500');
                            this.classList.remove('bg-gray-100', 'text-gray-600');
                            icon.classList.remove('far');
                            icon.classList.add('fas');
                        }
                    }
                } catch (error) {
                    console.error('Error toggling like:', error);
                }
            });
        }

        // Comment functionality
        const commentForm = document.getElementById('commentForm');
        const commentText = document.getElementById('commentText');
        const charCount = document.getElementById('charCount');
        
        if (commentText) {
            commentText.addEventListener('input', function() {
                charCount.textContent = `${this.value.length} / 500`;
            });
        }

        if (commentForm) {
            commentForm.addEventListener('submit', async function(e) {
                e.preventDefault();
                
                const text = commentText.value.trim();
                if (!text) return;
                
                const catchId = '<%= catchItem._id %>';
                
                try {
                    const response = await fetch(`/catches/${catchId}/comments`, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify({ text })
                    });

                    if (response.ok) {
                        const comments = await response.json();
                        
                        // Update comment count
                        document.getElementById('commentCount').textContent = comments.length;
                        
                        // Clear form
                        commentText.value = '';
                        charCount.textContent = '0 / 500';
                        
                        // Reload page to show new comment
                        location.reload();
                    } else {
                        const error = await response.json();
                        alert(error.msg || 'Error posting comment');
                    }
                } catch (error) {
                    console.error('Error posting comment:', error);
                    alert('Error posting comment. Please try again.');
                }
            });
        }
    </script>
</body>
</html>
