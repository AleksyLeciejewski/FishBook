<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - FishHub</title>
    <link rel="icon" type="image/png" href="/images/fishhub-logo.png">
    <link rel="shortcut icon" type="image/png" href="/images/fishhub-logo.png">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .trip-card {
            transition: all 0.3s ease;
        }
        .trip-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-10px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body class="bg-black min-h-screen">
    <!-- Navigation -->
    <nav class="bg-black text-white shadow-lg border-b border-yellow-400">
        <div class="container mx-auto px-4 py-3 flex justify-between items-center">
            <a href="/" class="flex items-center">
                <img src="/images/fishhub-logo.png" alt="FishHub" class="h-10">
            </a>
            <div class="flex items-center space-x-4">
                <% if (locals.currentUser) { %>
                    <a href="/catches" class="hover:bg-gray-800 px-3 py-2 rounded transition text-white">My Catches</a>
                    <a href="/trips" class="hover:bg-gray-800 px-3 py-2 rounded transition text-white">Trips</a>
                    <a href="/fishing-spots" class="hover:bg-gray-800 px-3 py-2 rounded transition text-white">Find Spots</a>
                    <div class="relative">
                        <button id="userMenuButton" class="flex items-center space-x-2 focus:outline-none hover:bg-gray-800 px-3 py-2 rounded transition">
                            <img src="<%= currentUser.profilePicture || '/images/default-avatar.png' %>" 
                                 alt="Profile" 
                                 class="w-8 h-8 rounded-full border-2 border-yellow-400">
                            <span><%= currentUser.username %></span>
                            <i class="fas fa-chevron-down text-xs"></i>
                        </button>
                        <div id="userMenuDropdown" class="hidden absolute right-0 mt-2 w-48 bg-gray-900 rounded-lg shadow-xl py-2 z-10 border border-gray-700">
                            <a href="/users/<%= currentUser.id || currentUser._id %>" class="block px-4 py-2 text-white hover:bg-gray-800 transition">
                                <i class="fas fa-user mr-2 text-yellow-400"></i>Profile
                            </a>
                            <a href="/settings" class="block px-4 py-2 text-white hover:bg-gray-800 transition">
                                <i class="fas fa-cog mr-2 text-yellow-400"></i>Settings
                            </a>
                            <form action="/auth/logout" method="POST" class="border-t border-gray-700 mt-1 pt-1">
                                <button type="submit" class="w-full text-left px-4 py-2 text-white hover:bg-gray-800 transition">
                                    <i class="fas fa-sign-out-alt mr-2 text-red-500"></i>Logout
                                </button>
                            </form>
                        </div>
                    </div>
                    <script>
                        document.addEventListener('DOMContentLoaded', function() {
                            const menuButton = document.getElementById('userMenuButton');
                            const menuDropdown = document.getElementById('userMenuDropdown');
                            if (menuButton && menuDropdown) {
                                menuButton.addEventListener('click', function(e) {
                                    e.stopPropagation();
                                    menuDropdown.classList.toggle('hidden');
                                });
                                document.addEventListener('click', function(e) {
                                    if (!menuButton.contains(e.target) && !menuDropdown.contains(e.target)) {
                                        menuDropdown.classList.add('hidden');
                                    }
                                });
                            }
                        });
                    </script>
                <% } else { %>
                    <a href="/auth/register" class="hover:bg-gray-800 px-3 py-2 rounded transition text-white">Register</a>
                    <a href="/auth/login" class="bg-yellow-400 text-black hover:bg-yellow-300 px-4 py-2 rounded font-bold transition">Login</a>
                <% } %>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <div class="bg-gray-900 text-white py-12 border-b border-gray-700">
        <div class="container mx-auto px-4">
            <div class="flex flex-col md:flex-row justify-between items-center">
                <div>
                    <h1 class="text-4xl font-bold mb-2">Fishing Trips</h1>
                    <p class="text-gray-300 text-lg">Join exciting fishing adventures with fellow anglers</p>
                </div>
                <% if (locals.currentUser) { %>
                    <a href="/trips/new" class="mt-4 md:mt-0 bg-orange-500 text-white hover:bg-orange-600 px-6 py-3 rounded-lg font-semibold flex items-center shadow-lg transition transform hover:scale-105">
                        <i class="fas fa-plus mr-2"></i> Plan a Trip
                    </a>
                <% } %>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <main class="container mx-auto px-4 py-8">
        <!-- Filter Section -->
        <div class="bg-gray-900 rounded-xl shadow-md p-6 mb-8 border border-gray-700">
            <form action="/trips" method="GET" class="space-y-4">
                <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
                    <!-- Location Search -->
                    <div class="md:col-span-2">
                        <label for="location" class="block text-sm font-medium text-gray-300 mb-2">
                            <i class="fas fa-map-marker-alt mr-1"></i> Location
                        </label>
                        <input type="text" 
                               name="location" 
                               id="location" 
                               class="w-full px-4 py-2 border border-gray-600 bg-gray-700 text-white rounded-lg focus:ring-2 focus:ring-orange-500 focus:border-orange-500 transition" 
                               placeholder="Search by location..."
                               value="<%= location || '' %>">
                    </div>
                    
                    <!-- Status Filter -->
                    <div>
                        <label for="status" class="block text-sm font-medium text-gray-300 mb-2">
                            <i class="fas fa-filter mr-1"></i> Status
                        </label>
                        <select id="status" 
                                name="status" 
                                class="w-full px-4 py-2 border border-gray-600 bg-gray-700 text-white rounded-lg focus:ring-2 focus:ring-orange-500 focus:border-orange-500 transition">
                            <option value="">All Trips</option>
                            <option value="upcoming" <%= status === 'upcoming' ? 'selected' : '' %>>Upcoming</option>
                            <option value="in_progress" <%= status === 'in_progress' ? 'selected' : '' %>>In Progress</option>
                            <option value="completed" <%= status === 'completed' ? 'selected' : '' %>>Completed</option>
                        </select>
                    </div>
                    
                    <!-- Submit Button -->
                    <div class="flex items-end">
                        <button type="submit" class="w-full bg-yellow-400 hover:bg-yellow-300 text-black px-6 py-2 rounded-lg font-medium transition transform hover:scale-105 shadow-md">
                            <i class="fas fa-search mr-2"></i> Search
                        </button>
                    </div>
                </div>
            </form>
        </div>

        <!-- Trips Grid -->
        <% if (trips && trips.length > 0) { %>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <% trips.forEach(trip => { %>
                    <div class="trip-card bg-gray-800 rounded-xl shadow-md overflow-hidden border border-gray-700 hover:border-yellow-400">
                        <!-- Header with Status Badge -->
                        <div class="relative h-48 bg-gradient-to-br from-green-400 to-teal-500 overflow-hidden">
                            <% if (trip.images && trip.images.length > 0) { %>
                                <img src="/<%= trip.images[0] %>" 
                                     alt="<%= trip.title %>" 
                                     class="w-full h-full object-cover">
                            <% } else { %>
                                <div class="flex items-center justify-center h-full">
                                    <i class="fas fa-ship text-white text-6xl opacity-50"></i>
                                </div>
                            <% } %>
                            <!-- Status Badge -->
                            <div class="absolute top-4 left-4">
                                <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-bold shadow-lg
                                    <%= trip.status === 'upcoming' ? 'bg-blue-500 text-white' : 
                                       trip.status === 'in_progress' ? 'bg-yellow-500 text-white' : 
                                       trip.status === 'completed' ? 'bg-green-500 text-white' : 
                                       'bg-red-500 text-white' %>">
                                    <i class="fas fa-circle text-xs mr-2"></i>
                                    <%= trip.status.replace('_', ' ').replace(/\b\w/g, l => l.toUpperCase()) %>
                                </span>
                            </div>
                            <!-- Creator Badge -->
                            <% if (currentUser && trip.createdBy && String(trip.createdBy._id) === String(currentUser._id)) { %>
                                <div class="absolute top-4 right-4">
                                    <span class="bg-white bg-opacity-95 text-gray-800 px-3 py-1 rounded-full text-sm font-semibold shadow-lg">
                                        <i class="fas fa-crown mr-1 text-yellow-500"></i> Your Trip
                                    </span>
                                </div>
                            <% } %>
                        </div>
                        
                        <!-- Content -->
                        <div class="p-5">
                            <!-- Title -->
                            <h3 class="font-bold text-xl text-white mb-3">
                                <a href="/trips/<%= trip._id %>" class="hover:text-yellow-400 transition">
                                    <%= trip.title %>
                                </a>
                            </h3>
                            
                            <!-- Location -->
                            <div class="flex items-center text-sm text-gray-300 mb-3">
                                <i class="fas fa-map-marker-alt mr-2 text-yellow-400"></i>
                                <span class="truncate"><%= trip.location.name %></span>
                            </div>
                            
                            <!-- Dates -->
                            <div class="flex items-center text-sm text-gray-300 mb-3">
                                <i class="far fa-calendar-alt mr-2 text-blue-500"></i>
                                <span>
                                    <%= new Date(trip.startDate).toLocaleDateString('en-US', { month: 'short', day: 'numeric' }) %> - 
                                    <%= new Date(trip.endDate).toLocaleDateString('en-US', { month: 'short', day: 'numeric' }) %>
                                </span>
                            </div>
                            
                            <!-- Description -->
                            <% if (trip.description) { %>
                                <p class="text-sm text-gray-600 mb-4 line-clamp-2">
                                    <%= trip.description.length > 100 ? 
                                        trip.description.substring(0, 100) + '...' : 
                                        trip.description %>
                                </p>
                            <% } %>
                            
                            <!-- Participants -->
                            <div class="flex items-center justify-between pt-4 border-t border-gray-700 mb-4">
                                <div class="flex items-center">
                                    <span class="text-sm font-semibold text-gray-400 mr-3">Participants:</span>
                                    <div class="flex -space-x-3">
                                        <% trip.participants.slice(0, 4).forEach(participant => { %>
                                            <img src="<%= participant.user.profilePicture || '/images/default-avatar.png' %>" 
                                                 alt="<%= participant.user.username %>" 
                                                 class="w-8 h-8 rounded-full border-2 border-white shadow-md hover:scale-110 transition-transform duration-200"
                                                 title="<%= participant.user.username %>">
                                        <% }); %>
                                        <% if (trip.participants.length > 4) { %>
                                            <span class="flex items-center justify-center w-8 h-8 rounded-full bg-gradient-to-br from-gray-300 to-gray-400 text-white text-xs font-bold border-2 border-white shadow-md">
                                                +<%= trip.participants.length - 4 %>
                                            </span>
                                        <% } %>
                                    </div>
                                </div>
                                <span class="text-sm text-gray-400">
                                    <%= trip.participants.length %>/<%= trip.maxParticipants %>
                                </span>
                            </div>
                            
                            <!-- View Details Button -->
                            <a href="/trips/<%= trip._id %>" class="block w-full text-center bg-yellow-400 hover:bg-yellow-300 text-black py-2 rounded-lg font-medium transition transform hover:scale-105">
                                View Details
                            </a>
                        </div>
                    </div>
                <% }); %>
            </div>
            
            <!-- Pagination -->
            <% if (totalPages > 1) { %>
                <div class="mt-12 flex justify-center">
                    <nav class="relative z-0 inline-flex rounded-lg shadow-md overflow-hidden" aria-label="Pagination">
                        <% if (currentPage > 1) { %>
                            <a href="?page=<%= currentPage - 1 %><%= location ? '&location=' + location : '' %><%= status ? '&status=' + status : '' %>" 
                               class="relative inline-flex items-center px-4 py-2 bg-white border-r border-gray-200 text-sm font-medium text-gray-700 hover:bg-blue-50 transition">
                                <i class="fas fa-chevron-left mr-2"></i> Previous
                            </a>
                        <% } %>
                        
                        <% for (let i = 1; i <= Math.min(totalPages, 5); i++) { %>
                            <% if (i === currentPage) { %>
                                <span class="relative inline-flex items-center px-4 py-2 bg-gradient-to-r from-blue-600 to-indigo-600 text-sm font-medium text-white">
                                    <%= i %>
                                </span>
                            <% } else { %>
                                <a href="?page=<%= i %><%= location ? '&location=' + location : '' %><%= status ? '&status=' + status : '' %>" 
                                   class="relative inline-flex items-center px-4 py-2 bg-white border-r border-gray-200 text-sm font-medium text-gray-700 hover:bg-blue-50 transition">
                                    <%= i %>
                                </a>
                            <% } %>
                        <% } %>
                        
                        <% if (currentPage < totalPages) { %>
                            <a href="?page=<%= currentPage + 1 %><%= location ? '&location=' + location : '' %><%= status ? '&status=' + status : '' %>" 
                               class="relative inline-flex items-center px-4 py-2 bg-white text-sm font-medium text-gray-700 hover:bg-blue-50 transition">
                                Next <i class="fas fa-chevron-right ml-2"></i>
                            </a>
                        <% } %>
                    </nav>
                </div>
            <% } %>
            
        <% } else { %>
            <!-- Empty State -->
            <div class="bg-yellow-900 rounded-xl shadow-md p-12 text-center border border-yellow-700">
                <div class="inline-flex items-center justify-center w-20 h-20 rounded-full bg-yellow-800 border-2 border-yellow-400 mb-6">
                    <i class="fas fa-ship text-4xl text-yellow-400"></i>
                </div>
                <h3 class="text-2xl font-bold text-black mb-2">No Trips Found</h3>
                <p class="text-yellow-400 mb-8 max-w-md mx-auto">
                    <% if (location || status) { %>
                        No trips match your current filters. Try adjusting your search criteria.
                    <% } else { %>
                        Be the first to organize a fishing trip and invite others to join!
                    <% } %>
                </p>
                <% if (locals.currentUser) { %>
                    <a href="/trips/new" class="inline-flex items-center px-6 py-3 bg-yellow-400 hover:bg-yellow-300 text-black font-semibold rounded-lg shadow-lg transition transform hover:scale-105">
                        <i class="fas fa-plus mr-2"></i> Plan Your First Trip
                    </a>
                <% } %>
            </div>
        <% } %>
    </main>
    
    <!-- Footer -->
    <footer class="bg-black border-t border-gray-800 mt-12">
        <div class="container mx-auto px-4 py-8">
            <div class="text-center text-gray-400">
                <p class="text-sm">
                    &copy; <%= new Date().getFullYear() %> FishHub. Made with <i class="fas fa-heart text-yellow-400"></i> by anglers, for anglers.
                </p>
            </div>
        </div>
    </footer>
</body>
</html>
